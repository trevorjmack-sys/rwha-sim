{
  "name": "rwha-sim",
  "version": "0.0.1",
  "description": "Hockey simulator for the Republik World Hockey Association — a private 22-team fantasy league.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "sim": "tsx src/cli.ts",
    "season": "tsx src/season-cli.ts",
    "calibrate": "tsx src/calibrate.ts",
    "db:seed": "tsx scripts/seed.ts",
    "db:migrate:local": "npx wrangler d1 execute rwha-sim --local --file=migrations/0001_initial.sql",
    "db:migrate:remote": "npx wrangler d1 execute rwha-sim --file=migrations/0001_initial.sql",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
